diff --git a/src/astro/routes/api/auth/oauth/[provider]/callback.ts b/src/astro/routes/api/auth/oauth/[provider]/callback.ts
index a9775edd29284fcbb54fe12eaa270c2ea3d20b3c..912e0ba8d5ec11207efc3671fa322a00c56646d5 100644
--- a/src/astro/routes/api/auth/oauth/[provider]/callback.ts
+++ b/src/astro/routes/api/auth/oauth/[provider]/callback.ts
@@ -116,9 +116,18 @@ export const GET: APIRoute = async ({ params, request, locals, session, redirect
 
 	try {
 		// Get OAuth providers from environment
-		// eslint-disable-next-line typescript/no-unsafe-type-assertion -- locals.runtime is injected by the Cloudflare adapter at runtime; not declared on App.Locals since the adapter is optional
-		const runtimeLocals = locals as unknown as { runtime?: { env?: Record<string, unknown> } };
-		const env = runtimeLocals.runtime?.env ?? import.meta.env;
+		// Astro v6 removed locals.runtime.env. Read bindings via cloudflare:workers
+		// on Workers, falling back to import.meta.env in dev (Node).
+		// Local patch — see patches/emdash@0.29.0.patch.
+		let env: Record<string, unknown>;
+		try {
+			const cf = await import("cloudflare:workers");
+			// eslint-disable-next-line typescript/no-unsafe-type-assertion
+			env = cf.env as Record<string, unknown>;
+		} catch {
+			// eslint-disable-next-line typescript/no-unsafe-type-assertion
+			env = import.meta.env as Record<string, unknown>;
+		}
 		const providers = getOAuthConfig(env);
 
 		if (!providers[provider]) {
diff --git a/src/astro/routes/api/auth/oauth/[provider].ts b/src/astro/routes/api/auth/oauth/[provider].ts
index c9a29dc8522e7a94d272799ee8c23797ded5ef74..f3f117a924621d3061f93b90ced591aee8b7e2ae 100644
--- a/src/astro/routes/api/auth/oauth/[provider].ts
+++ b/src/astro/routes/api/auth/oauth/[provider].ts
@@ -94,10 +94,18 @@ export const GET: APIRoute = async ({ params, request, locals, redirect }) => {
 		const url = new URL(request.url);
 
 		// Get OAuth providers from environment
-		// Access via locals.runtime for Cloudflare, or import.meta.env for Node
-		// eslint-disable-next-line typescript/no-unsafe-type-assertion -- locals.runtime is injected by the Cloudflare adapter at runtime; not declared on App.Locals since the adapter is optional
-		const runtimeLocals = locals as unknown as { runtime?: { env?: Record<string, unknown> } };
-		const env = runtimeLocals.runtime?.env ?? import.meta.env;
+		// Astro v6 removed locals.runtime.env. Read bindings via cloudflare:workers
+		// on Workers, falling back to import.meta.env in dev (Node).
+		// Local patch — see patches/emdash@0.29.0.patch.
+		let env: Record<string, unknown>;
+		try {
+			const cf = await import("cloudflare:workers");
+			// eslint-disable-next-line typescript/no-unsafe-type-assertion
+			env = cf.env as Record<string, unknown>;
+		} catch {
+			// eslint-disable-next-line typescript/no-unsafe-type-assertion
+			env = import.meta.env as Record<string, unknown>;
+		}
 		const providers = getOAuthConfig(env);
 
 		if (!providers[provider]) {
diff --git a/src/components/InlinePortableTextEditor.tsx b/src/components/InlinePortableTextEditor.tsx
index 2d1216220d3cd5a20e92db0187213e3b696226f8..c16cca5b07f8f1bb8a5b44608454ccdec0bf93f5 100644
--- a/src/components/InlinePortableTextEditor.tsx
+++ b/src/components/InlinePortableTextEditor.tsx
@@ -26,6 +26,10 @@ import * as React from "react";
 import { createPortal } from "react-dom";
 
 import { computeThumbnailSize } from "../media/thumbnail.js";
+import {
+	pluginBlockAttrsToPortableTextBlock,
+	portableTextPluginBlockToAttrs,
+} from "./plugin-block-attrs.js";
 import { InlineCodeBlockExtension } from "./inline-code-block.js";
 
 // ── Portable Text types ────────────────────────────────────────────
@@ -227,18 +231,8 @@ function convertPMNode(node: PMNode): PTBlock | PTBlock[] | null {
 		}
 		case "horizontalRule":
 			return { _type: "break", _key: k(), style: "lineBreak" };
-		case "pluginBlock": {
-			// Spread the captured data back out so the block round-trips losslessly.
-			// `data` holds every field except _type / _key / id (which live on
-			// dedicated attrs).
-			const { blockType, id, data } = node.attrs ?? {};
-			return {
-				...(data && typeof data === "object" ? data : {}),
-				_type: typeof blockType === "string" ? blockType : "embed",
-				_key: k(),
-				id: typeof id === "string" ? id : "",
-			};
-		}
+		case "pluginBlock":
+			return pluginBlockAttrsToPortableTextBlock(node.attrs, k);
 		default:
 			return null;
 	}
@@ -509,23 +503,9 @@ function convertPTBlock(block: PTBlock): JSONContent | null {
 			},
 		};
 	}
-	// Unknown block types — treat as plugin blocks. Capture every field other
-	// than the well-known ones into `data` so the block round-trips losslessly,
-	// even if no plugin currently registers this type. Matches the admin
-	// editor's behaviour at PortableTextEditor.tsx:572-588.
-	const { _type, _key, id, url, ...rest } = block as { _type: string; _key: string } & Record<
-		string,
-		unknown
-	>;
-	// Filter out _-prefixed keys to prevent accumulation across edit cycles.
-	const data = Object.fromEntries(Object.entries(rest).filter(([key]) => !key.startsWith("_")));
 	return {
 		type: "pluginBlock",
-		attrs: {
-			blockType: typeof _type === "string" ? _type : "embed",
-			id: typeof id === "string" ? id : typeof url === "string" ? url : "",
-			data,
-		},
+		attrs: portableTextPluginBlockToAttrs(block),
 	};
 }
 
@@ -944,6 +924,7 @@ const PluginBlockNode = Node.create({
 		const noDom = { rendered: false, parseHTML: () => null };
 		return {
 			blockType: { default: "", ...noDom },
+			blockKey: { default: "", ...noDom },
 			id: { default: "", ...noDom },
 			data: { default: {}, ...noDom },
 		};
diff --git a/src/components/plugin-block-attrs.ts b/src/components/plugin-block-attrs.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4834ae362ea5c5d6bd05194cf85dcd3376fc36e1
--- /dev/null
+++ b/src/components/plugin-block-attrs.ts
@@ -0,0 +1,53 @@
+export interface PortableTextPluginBlock {
+	_type: string;
+	_key: string;
+	[key: string]: unknown;
+}
+
+export interface PluginBlockAttrs extends Record<string, unknown> {
+	blockType: string;
+	blockKey?: string;
+	id?: string;
+	data?: Record<string, unknown>;
+}
+
+function isPlainObject(value: unknown): value is Record<string, unknown> {
+	return typeof value === "object" && value !== null && !Array.isArray(value);
+}
+
+function omitPrivateKeys(value: Record<string, unknown>): Record<string, unknown> {
+	return Object.fromEntries(Object.entries(value).filter(([key]) => !key.startsWith("_")));
+}
+
+export function portableTextPluginBlockToAttrs(block: PortableTextPluginBlock): PluginBlockAttrs {
+	const { _type, _key, id, url, ...rest } = block;
+	return {
+		blockType: typeof _type === "string" && _type ? _type : "embed",
+		blockKey: _key,
+		id: typeof id === "string" ? id : typeof url === "string" ? url : "",
+		data: omitPrivateKeys(rest),
+	};
+}
+
+export function pluginBlockAttrsToPortableTextBlock(
+	attrs: Record<string, unknown> | undefined,
+	generateKey: () => string,
+): PortableTextPluginBlock {
+	const input = isPlainObject(attrs) ? attrs : {};
+	const { blockType, blockKey, type, id, data, ...rest } = input;
+	const resolvedType =
+		typeof blockType === "string" && blockType
+			? blockType
+			: typeof type === "string" && type
+				? type
+				: "embed";
+	const resolvedKey = typeof blockKey === "string" && blockKey ? blockKey : generateKey();
+
+	return {
+		...(isPlainObject(data) ? data : {}),
+		...omitPrivateKeys(rest),
+		_type: resolvedType,
+		_key: resolvedKey,
+		id: typeof id === "string" ? id : "",
+	};
+}
diff --git a/src/components/widgets/RecentPosts.astro b/src/components/widgets/RecentPosts.astro
index cef0090a0aeaf52c8b0b74f6505fdb73b57aa94f..3829d30844384c693358672cdb92b46b3c5590bc 100644
--- a/src/components/widgets/RecentPosts.astro
+++ b/src/components/widgets/RecentPosts.astro
@@ -9,7 +9,10 @@ interface Props {
 
 const { count = 5, showThumbnails = false, showDate = true } = Astro.props;
 
+const currentLocale = Astro.currentLocale ?? "en";
+
 const { entries: posts } = await getEmDashCollection("posts", {
+	locale: currentLocale,
 	limit: count,
 	orderBy: { published_at: "desc" },
 });
@@ -19,6 +22,11 @@ function getString(data: Record<string, unknown>, key: string): string | undefin
 	const val = data[key];
 	return typeof val === "string" ? val : undefined;
 }
+
+function getPostHref(id: string): string {
+	const prefix = currentLocale === "en" ? "" : `/${currentLocale}`;
+	return `${prefix}/posts/${id}`;
+}
 ---
 
 <ul class="widget-recent-posts">
@@ -36,7 +44,7 @@ function getString(data: Record<string, unknown>, key: string): string | undefin
 							class="widget-recent-posts__thumbnail"
 						/>
 					)}
-					<a href={`/posts/${post.id}`} class="widget-recent-posts__link">
+					<a href={getPostHref(post.id)} class="widget-recent-posts__link">
 						{title}
 					</a>
 					{showDate && publishedAt && (
