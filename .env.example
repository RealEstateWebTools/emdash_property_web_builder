# The URL of your PWB backend — no trailing slash
PWB_API_URL=http://localhost:3000

# Optional: API key if your PWB instance requires it (most don't for public endpoints)
# PWB_API_KEY=your-key-here
