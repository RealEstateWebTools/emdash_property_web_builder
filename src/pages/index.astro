---
import IndexPage from '../components/pages/IndexPage.astro'
import { resolveDefaultLocale } from '../lib/route-locale'

const locale = resolveDefaultLocale(Astro.currentLocale)
---

<IndexPage locale={locale} />
