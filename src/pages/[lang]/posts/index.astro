---
import PostsIndexPage from '../../../components/pages/PostsIndexPage.astro'
import { resolveLocalizedLocale } from '../../../lib/route-locale'

const locale = resolveLocalizedLocale(Astro)
---

{locale ? <PostsIndexPage locale={locale} /> : null}
