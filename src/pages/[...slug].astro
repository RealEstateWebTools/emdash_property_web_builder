---
import PwbPage from '../components/pages/PwbPage.astro'
import { DEFAULT_LOCALE } from '../lib/locale'

const locale = Astro.currentLocale ?? DEFAULT_LOCALE
const { slug } = Astro.params
---

<PwbPage locale={locale} slug={slug} />
