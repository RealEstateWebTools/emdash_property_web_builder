---
import CmsPage from '../../components/pages/CmsPage.astro'
import { DEFAULT_LOCALE } from '../../lib/locale'

const locale = Astro.currentLocale ?? DEFAULT_LOCALE
const { slug } = Astro.params
---

{slug ? <CmsPage locale={locale} slug={slug} /> : null}
