---
import PropertyIndexPage from '../../components/pages/PropertyIndexPage.astro'
import { DEFAULT_LOCALE } from '../../lib/locale'

const locale = Astro.currentLocale ?? DEFAULT_LOCALE
---

<PropertyIndexPage locale={locale} />
